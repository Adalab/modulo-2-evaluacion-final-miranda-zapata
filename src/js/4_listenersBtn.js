'use strict';
// Listeners (eventos clic) de los botones
btnSubmit.addEventListener('click', handleInputSearch);
btnReset.addEventListener('click', handleClickReset);
btnDislikeAll.addEventListener('click', handleDislikeAllFavs);
